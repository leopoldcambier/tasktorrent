sudo: required
dist: xenial
before_install:
  - sudo apt-get install openmpi-bin libopenmpi-dev
language: cpp
compiler:
  - gcc
install:
  - wget http://bitbucket.org/eigen/eigen/get/3.3.7.tar.bz2 -O eigen-3.3.7.tar.bz2 && mkdir eigen && tar -xvf eigen-3.3.7.tar.bz2 -C eigen --strip-components 1
script:
  - EIGEN3_ROOT=${HOME}/build/leopoldcambier/tasktorrent/eigen ./tests/test_all.sh $(pwd) OFF